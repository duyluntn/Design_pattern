Template Method Pattern được áp dụng để định nghĩa ra cấu trúc chung, bộ khung (hay còn gọi skeleton – xương sống)
chung cho một xử lý nào đó ở class cha (base class) và cho phép các lớp con (subclass) định nghĩa lại một số step trong
bộ khung mà không làm thay đổi cấu trúc chung của xử lý.